<!DOCTYPE html>
<html lang="en">
{{ template "head" . }}

<body>

{{ template "navbar" . }}

<main class="py-8">
    <div class="mx-auto w-full text-center max-w-sm">
        <h1 class="text-3xl brand">Create a new account</h1>
        <p>Please register with your email account.</p>
        <form class="py-6 mb-4 flex items-center justify-center flex-col border-b">
            <input type="text" placeholder="Email" class="shadow rounded-lg p-2 w-full mb-2 text-sm"
                   required/>
            <input type="password" placeholder="Password" class="shadow rounded-lg p-2 w-full text-sm mb-4"
                   required/>
            <button class="rounded-lg bg-gray-800 w-full text-white p-2 px-4 text-sm font-medium">Sign up with Email</button>
        </form>
        <p class="mb-4">Or use one of the following providers:</p>
        <button class="rounded-lg w-full bg-white p-2 px-4 text-sm shadow-sm font-medium mb-2">Sign up with GitHub
        </button>
        <button class="rounded-lg bg-rose-500 w-full text-white p-2 px-4 text-sm shadow-sm  font-medium mb-2">Sign up with Google
        </button>
        <button class="rounded-lg bg-black w-full text-white p-2 px-4 text-sm  shadow-sm font-medium mb-2">Sign up with Apple
        </button>
    </div>
</main>

</body>

</html>
